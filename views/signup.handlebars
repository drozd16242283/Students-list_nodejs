<div class="container">
    <div class="col-sm-6 col-sm-offset-3">
        <h1><span class="fa fa-sign-in"></span> Зарегистрироваться</h1>

        <form action="/signup" method="post">
            <div class="form-group">
                <label>Логин</label>
                <input type="text" class="form-control" name="usernameSignup" required />
            </div>
            <div class="form-group">
                <label>Пароль</label>
                <input type="password" class="form-control" name="passwordSignup" required />
            </div>

            <button type="submit" class="btn btn-warning btn-lg">Зарегистрироваться</button>

        </form>

        {{#if error}}
            <hr />
            <div class="alert-danger"><h3>{{error}}</h3></div>
        {{/if}}

        <hr />

        <p>Уже есть аккаунт? <a href="/signin">Войти</a></p>
        <p>Вернуться на главную <a href="/">Главная</a></p>
    </div>
</div>


